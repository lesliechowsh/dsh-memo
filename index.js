// Memo (dsh-memo) — host-side placeholder. The functional plugin (memo_search /
// memo_remember / memo_stats tools over the official sessionQuery service) is
// under construction; this entry exists so the package name is reserved and the
// package mounts cleanly in the meantime.
exports.name = "dsh-memo";
exports.apply = function () {};
